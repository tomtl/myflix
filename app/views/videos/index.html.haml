- @categories.each do |category|
  %article.video_category
    %header
      %h3= category.name.pluralize
    .videos.row
      - category.videos.each do |video|
        .video.col-sm-2
          = link_to image_tag("/tmp/#{video.small_cover_url}"), video_path(video)

