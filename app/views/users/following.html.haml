%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @user.following.each do |followed_user|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(#{followed_user.email}.downcase)}?s=40")
            = link_to folowee.fullname, user_path(followed_user)
          %td.extra-padding 20
          %td.extra-padding 30
          %td.extra-padding
            = link_to unfollow_path(followed_user), method: :post do
              %i.glyphicon.glyphicon-remove