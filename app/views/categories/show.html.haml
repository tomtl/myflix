%section.genre
  %header.clearfix
    %h1 #{@category.name}
  %article.row
    - @category.videos.all.each do |video|
      .video.col-md-2
        = link_to image_tag("#{video.small_cover_url}"), video_path(video)